import React from 'react'

// would be rendered using virtual main

export default function App() {
  return (<React.StrictMode>
    This is App on page-name which is transformed.
  </React.StrictMode>);
}
